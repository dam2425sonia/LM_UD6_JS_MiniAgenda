# Objetivo: 
Crear una pequeña aplicación donde se puedan
agregar tareas, marcarlas como completadas y borrarlas, usando HTML limpio
y JS separado.

Tener en cuenta que la página web que tenemos que crear, HTML, sea similar a la de la imagen.
Y en el JS tendremos que desarrollar todo lo que pide el ejercicio.
